/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package with.submodules.app;

import with.submodules.list.LinkedList;

import static with.submodules.utilities.StringUtils.join;
import static with.submodules.utilities.StringUtils.split;
import static with.submodules.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
